---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/layout/Header';
import Footer from '../../components/layout/Footer';
import { ChevronRight, Phone, Mail, CheckCircle2, ArrowLeft } from "lucide-react";

export function getStaticPaths() {
  const units = [
    "dahiliye", "kardiyoloji", "noroloji", "ortopedi",
    "cocuk-sagligi", "kadin-hastaliklari", "genel-cerrahi", "goz-hastaliklari",
    "kulak-burun-bogaz", "dermatoloji", "fizik-tedavi", "uroloji"
  ];
  return units.map(slug => ({ params: { slug } }));
}

const { slug } = Astro.params;

// Simple formatter for the title
const title = slug
    .split("-")
    .map((word) => word.charAt(0).toUpperCase() + word.slice(1))
    .join(" ");

const otherUnits = [
    "Kardiyoloji", "Nöroloji", "Ortopedi", "Göz Hastalıkları",
    "Çocuk Sağlığı", "Genel Cerrahi", "Dahiliye", "Dermatoloji"
];
---

<BaseLayout title={`${title} | İzmir Özel Tedavi`}>
    <Header client:load />

    <div class="bg-white min-h-screen">
        {/* Header / Breadcrumb Section */}
        <section class="bg-primary pt-32 pb-20 text-white relative overflow-hidden">
            <div class="absolute inset-0 bg-action/5 pointer-events-none"></div>
            <div class="container mx-auto px-4 relative z-10">
                <a href="/" class="inline-flex items-center text-action font-bold text-sm mb-6 hover:translate-x-1 transition-transform">
                    <ArrowLeft class="w-4 h-4 mr-2" /> ANA SAYFAYA DÖN
                </a>
                <h1 class="text-4xl md:text-6xl font-bold font-heading uppercase mb-6">{title}</h1>
                <div class="flex items-center text-gray-400 text-sm font-bold tracking-widest uppercase">
                    <span>Ana Sayfa</span>
                    <ChevronRight class="w-4 h-4 mx-2 text-action" />
                    <span>Tıbbi Birimler</span>
                    <ChevronRight class="w-4 h-4 mx-2 text-action" />
                    <span class="text-white">{title}</span>
                </div>
            </div>
        </section>

        {/* Content Area */}
        <section class="py-20">
            <div class="container mx-auto px-4">
                <div class="flex flex-col lg:flex-row gap-16">

                    {/* Main Content */}
                    <div class="w-full lg:w-2/3 space-y-12">

                        {/* Introduction Section */}
                        <div class="prose prose-lg max-w-none prose-slate">
                            <h2 class="text-3xl font-bold text-primary font-heading border-l-4 border-action pl-6 mb-8 uppercase italic">Nedir?</h2>
                            <p class="text-gray-600 leading-relaxed text-lg">
                                {title}, günümüz tıbbının en önemli uzmanlık alanlarından biridir. İzmir Özel Tedavi olarak, bu birimde en gelişmiş tanı ve tedavi yöntemlerini kullanarak hastalarımıza dünya standartlarında hizmet sunuyoruz. Uzman hekim kadromuz ve modern teknolojik altyapımız ile sağlığınız her şeyin üstünde tutuyoruz.
                            </p>
                            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mt-8">
                                <div class="bg-slate-50 p-6 rounded-2xl border border-slate-100 italic font-medium text-primary uppercase tracking-tighter shadow-sm shadow-blue-500/5">
                                    "Modern Tıp, Uzman Bakış Açısı"
                                </div>
                                <div class="bg-slate-50 p-6 rounded-2xl border border-slate-100 italic font-medium text-primary uppercase tracking-tighter shadow-sm shadow-blue-500/5">
                                    "İzmir Özel Tedavi Güvencesiyle"
                                </div>
                            </div>
                        </div>

                        {/* Process Section */}
                        <div class="prose prose-lg max-w-none prose-slate">
                            <h2 class="text-3xl font-bold text-primary font-heading border-l-4 border-action pl-6 mb-8 uppercase italic">Süreç Nasıl İlerler?</h2>
                            <p class="text-gray-600 leading-relaxed text-lg">
                                Tedavi sürecimiz tamamen hasta odaklı bir yaklaşımla planlanır. İlk muayeneden itibaren hastalarımız her adımda bilgilendirilir ve kendilerine en uygun tedavi protokolü belirlenir.
                            </p>
                            <div class="space-y-4 mt-8">
                                <div class="flex items-start gap-4">
                                    <div class="w-6 h-6 rounded-full bg-action/10 flex items-center justify-center text-action mt-1 shrink-0">
                                        <CheckCircle2 className="w-4 h-4" />
                                    </div>
                                    <span class="text-gray-700 font-medium">Kapsamlı bir ön değerlendirme ve tetkik süreci.</span>
                                </div>
                                <div class="flex items-start gap-4">
                                    <div class="w-6 h-6 rounded-full bg-action/10 flex items-center justify-center text-action mt-1 shrink-0">
                                        <CheckCircle2 className="w-4 h-4" />
                                    </div>
                                    <span class="text-gray-700 font-medium">Uzman konsültasyonları ile en doğru tanının konulması.</span>
                                </div>
                                <div class="flex items-start gap-4">
                                    <div class="w-6 h-6 rounded-full bg-action/10 flex items-center justify-center text-action mt-1 shrink-0">
                                        <CheckCircle2 className="w-4 h-4" />
                                    </div>
                                    <span class="text-gray-700 font-medium">Kişiye özel tedavi planının oluşturulması ve uygulanması.</span>
                                </div>
                            </div>
                        </div>

                        {/* Recovery Section */}
                        <div class="prose prose-lg max-w-none prose-slate">
                            <h2 class="text-3xl font-bold text-primary font-heading border-l-4 border-action pl-6 mb-8 uppercase italic">İyileşme Dönemi</h2>
                            <p class="text-gray-600 leading-relaxed text-lg">
                                Taburcu sonrası takip ve rehabilitasyon süreçlerimiz, hastalarımızın günlük yaşamlarına en kısa sürede ve en sağlıklı şekilde dönmelerini sağlamak amacıyla titizlikle yönetilir.
                            </p>
                        </div>

                        {/* Embedded CTA Block */}
                        <div class="bg-primary rounded-[32px] p-10 mt-16 text-white relative overflow-hidden shadow-2xl shadow-blue-900/40">
                            <div class="absolute top-0 right-0 w-64 h-64 bg-action/10 rounded-full -mr-32 -mt-32 blur-3xl"></div>
                            <div class="relative z-10 flex flex-col md:flex-row items-center justify-between gap-12">
                                <div class="text-center md:text-left">
                                    <h3 class="text-3xl font-bold font-heading mb-4 uppercase leading-tight">Bu Tedavi Hakkında <br /> <span class="text-action underline underline-offset-8 decoration-4 decoration-action/20">Bilgi Almak İster Misiniz?</span></h3>
                                    <p class="text-gray-400 font-medium">Hemen randevu oluşturun veya uzmanlarımıza danışın.</p>
                                </div>
                                <div class="flex flex-col sm:flex-row gap-4 w-full md:w-auto">
                                    <button class="bg-action hover:bg-action-hover text-white px-8 py-5 rounded-xl font-bold transition-all shadow-lg shadow-sky-500/20 whitespace-nowrap uppercase tracking-widest text-sm">
                                        Hemen Görüşelim
                                    </button>
                                    <button class="bg-white/5 border border-white/10 hover:bg-white/10 text-white px-8 py-5 rounded-xl font-bold transition-all whitespace-nowrap uppercase tracking-widest text-sm">
                                        Bizi Arayın
                                    </button>
                                </div>
                            </div>
                        </div>

                    </div>

                    {/* Sidebar */}
                    <aside class="w-full lg:w-1/3 space-y-10">

                        {/* Other Services List */}
                        <div class="bg-slate-50 p-8 rounded-[32px] border border-slate-100 shadow-sm shadow-blue-500/5">
                            <h4 class="text-xl font-bold text-primary mb-8 font-heading border-b border-slate-200 pb-4 uppercase tracking-tighter italic">DİĞER BİRİMLER</h4>
                            <div class="flex flex-col space-y-1">
                                {otherUnits.map((unit) => (
                                    <a
                                        href={`/services/${unit.toLowerCase().replace(/ /g, "-").replace(/ı/g, "i").replace(/ğ/g, "g").replace(/ü/g, "u").replace(/ş/g, "s").replace(/ö/g, "o").replace(/ç/g, "c")}`}
                                        class="flex items-center justify-between p-4 rounded-xl hover:bg-white hover:text-action hover:shadow-sm text-gray-600 font-bold transition-all group"
                                    >
                                        <span class="uppercase tracking-tighter">{unit}</span>
                                        <ChevronRight class="w-4 h-4 opacity-0 group-hover:opacity-100 group-hover:translate-x-1 transition-all" />
                                    </a>
                                ))}
                            </div>
                        </div>

                        {/* Quick Contact Card */}
                        <div class="bg-action p-8 rounded-[32px] text-white shadow-xl shadow-sky-500/20">
                            <h4 class="text-2xl font-bold mb-6 font-heading uppercase italic tracking-tighter">Acil Yardım</h4>
                            <div class="space-y-6">
                                <div class="flex items-center gap-4">
                                    <div class="w-12 h-12 rounded-2xl bg-white/20 flex items-center justify-center">
                                        <Phone class="w-6 h-6" />
                                    </div>
                                    <div>
                                        <p class="text-sm font-bold uppercase opacity-70">Bizi Arayın</p>
                                        <p class="text-xl font-bold">444 0 444</p>
                                    </div>
                                </div>
                                <div class="flex items-center gap-4">
                                    <div class="w-12 h-12 rounded-2xl bg-white/20 flex items-center justify-center">
                                        <Mail class="w-6 h-6" />
                                    </div>
                                    <div>
                                        <p class="text-sm font-bold uppercase opacity-70">E-Posta</p>
                                        <p class="text-lg font-bold">iletisim@gazihastanesi.com</p>
                                    </div>
                                </div>
                            </div>
                            <button class="w-full bg-white text-action mt-8 py-5 rounded-2xl font-bold hover:bg-slate-50 transition-colors uppercase tracking-widest text-sm shadow-inner">
                                Tüm İletişim Bilgileri
                            </button>
                        </div>

                    </aside>

                </div>
            </div>
        </section>
    </div>

    <Footer />
</BaseLayout>
